# Local development only (netlify dev / vercel dev). Copy to .env; never commit .env.
# In production, set these in the Netlify or Vercel dashboard instead.
ANTHROPIC_API_KEY=
# Optional: override the concierge model
# ANTHROPIC_MODEL=
