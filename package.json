{
  "name": "all-in-1-events",
  "version": "1.0.0",
  "description": "Luxury event production website with AI concierge chatbot",
  "main": "index.html",
  "scripts": {
    "dev": "python -m http.server 8000",
    "start": "netlify dev",
    "lint": "echo 'Install ESLint to lint JS files'",
    "build:drone": "cd drone && npm ci --no-audit --no-fund && npm run build && rm -rf node_modules",
    "dev:drone": "cd drone && npm install && npm run dev",
    "build": "npm run build:drone && node scripts/build-site.mjs"
  },
  "keywords": [
    "events",
    "luxury",
    "chatbot",
    "ai",
    "concierge"
  ],
  "author": "Otis Williams",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/infoallin1eventsllc-sys/all-in-1-events-2"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
